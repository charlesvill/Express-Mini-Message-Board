<div class="msgCollection">
    <ul>
        <% console.log(params) %>
        <% params.messages.map((message) => { %>
            <li>
                <div class="msgCont all">
                    <div class="textCont all">
                        <p><%= message.content  %></p>
                    </div>
                    <div class="userCont all">
                        <%= message.name%>
                    </div>
                    <form action=<%=message.id%> method="GET">
                        <button type="submit">details</button>
                    </form>
                </div>
            </li>
        <% }) %>
    </ul>
</div>

